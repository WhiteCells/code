#include "../../head_file.h"

/*
problme: 

url: 
 */

class Solution {
public:
    vector<int> divisibilityArray(string word, int m) {
        // dp[i] % m
        // (dp[i] * 10 + dp[i + 1]) % m ?
    }
};

int main() {
    
    return 0;
}